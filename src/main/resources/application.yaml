spring:
  application:
    name: hr-system

  datasource:
    url: jdbc:postgresql://localhost:5432/hr_db
    username: postgres
    password: secret

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration